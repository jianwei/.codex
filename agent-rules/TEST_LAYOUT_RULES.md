# 测试目录布局规则

新增、迁移或修改测试代码时，统一遵循以下目录布局：

- 所有测试代码放在项目根目录的 `__tests__/` 中。
- `__tests__/` 内的目录路径镜像被测试代码相对项目根目录的目录路径。例如，被测试文件为 `src/services/user.ts` 时，测试文件放在 `__tests__/src/services/` 中。
- 测试文件名保留项目现有的测试后缀和命名约定，仅统一测试代码所在目录。
